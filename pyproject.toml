[project]
name = "naox"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "qi>=3.1.5",
]

[project.scripts]
naox = "naox:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
